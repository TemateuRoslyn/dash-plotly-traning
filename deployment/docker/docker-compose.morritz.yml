version: '3.7'

services:
   dashboard-plotly-morritz:
     build: 
       context: ./../../modules/morritz/src/frontend/python
     restart: always
     ports:
       - 4000:8050
     networks:
       - network 
    
networks:
  network:
    driver: bridge